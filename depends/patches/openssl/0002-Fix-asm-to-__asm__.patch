diff --git a/crypto/bn/asm/x86_64-gcc.c b/crypto/bn/asm/x86_64-gcc.c
index aa94a13..e379b48 100644
--- a/crypto/bn/asm/x86_64-gcc.c
+++ b/crypto/bn/asm/x86_64-gcc.c
@@ -65,15 +65,15 @@
  */
 # define mul_add(r,a,word,carry) do {   \
         register BN_ULONG high,low;     \
-        asm ("mulq %3"                  \
+        __asm__ ("mulq %3"                  \
                 : "=a"(low),"=d"(high)  \
                 : "a"(word),"m"(a)      \
                 : "cc");                \
-        asm ("addq %2,%0; adcq %3,%1"   \
+        __asm__ ("addq %2,%0; adcq %3,%1"   \
                 : "+r"(carry),"+d"(high)\
                 : "a"(low),"g"(0)       \
                 : "cc");                \
-        asm ("addq %2,%0; adcq %3,%1"   \
+        __asm__ ("addq %2,%0; adcq %3,%1"   \
                 : "+m"(r),"+d"(high)    \
                 : "r"(carry),"g"(0)     \
                 : "cc");                \
@@ -82,11 +82,11 @@
 
 # define mul(r,a,word,carry) do {       \
         register BN_ULONG high,low;     \
-        asm ("mulq %3"                  \
+        __asm__ ("mulq %3"                  \
                 : "=a"(low),"=d"(high)  \
                 : "a"(word),"g"(a)      \
                 : "cc");                \
-        asm ("addq %2,%0; adcq %3,%1"   \
+        __asm__ ("addq %2,%0; adcq %3,%1"   \
                 : "+r"(carry),"+d"(high)\
                 : "a"(low),"g"(0)       \
                 : "cc");                \
@@ -94,7 +94,7 @@
         } while (0)
 # undef sqr
 # define sqr(r0,r1,a)                   \
-        asm ("mulq %2"                  \
+        __asm__ ("mulq %2"                  \
                 : "=a"(r0),"=d"(r1)     \
                 : "a"(a)                \
                 : "cc");
@@ -187,7 +187,7 @@ BN_ULONG bn_div_words(BN_ULONG h, BN_ULONG l, BN_ULONG d)
 {
     BN_ULONG ret, waste;
 
- asm("divq      %4":"=a"(ret), "=d"(waste)
+ __asm__("divq      %4":"=a"(ret), "=d"(waste)
  :     "a"(l), "d"(h), "r"(d)
  :     "cc");
 
@@ -203,7 +203,7 @@ BN_ULONG bn_add_words(BN_ULONG *rp, const BN_ULONG *ap, const BN_ULONG *bp,
     if (n <= 0)
         return 0;
 
-    asm volatile ("       subq    %0,%0           \n" /* clear carry */
+    __asm__ volatile ("       subq    %0,%0           \n" /* clear carry */
                   "       jmp     1f              \n"
                   ".p2align 4                     \n"
                   "1:     movq    (%4,%2,8),%0    \n"
@@ -229,7 +229,7 @@ BN_ULONG bn_sub_words(BN_ULONG *rp, const BN_ULONG *ap, const BN_ULONG *bp,
     if (n <= 0)
         return 0;
 
-    asm volatile ("       subq    %0,%0           \n" /* clear borrow */
+    __asm__ volatile ("       subq    %0,%0           \n" /* clear borrow */
                   "       jmp     1f              \n"
                   ".p2align 4                     \n"
                   "1:     movq    (%4,%2,8),%0    \n"
@@ -338,11 +338,11 @@ BN_ULONG bn_sub_words(BN_ULONG *r, BN_ULONG *a, BN_ULONG *b, int n)
 # else
 #  define mul_add_c(a,b,c0,c1,c2) do {  \
         BN_ULONG t1,t2;                 \
-        asm ("mulq %3"                  \
+        __asm__ ("mulq %3"                  \
                 : "=a"(t1),"=d"(t2)     \
                 : "a"(a),"m"(b)         \
                 : "cc");                \
-        asm ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
+        __asm__ ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
                 : "+r"(c0),"+r"(c1),"+r"(c2)            \
                 : "r"(t1),"r"(t2),"g"(0)                \
                 : "cc");                                \
@@ -350,11 +350,11 @@ BN_ULONG bn_sub_words(BN_ULONG *r, BN_ULONG *a, BN_ULONG *b, int n)
 
 #  define sqr_add_c(a,i,c0,c1,c2) do {  \
         BN_ULONG t1,t2;                 \
-        asm ("mulq %2"                  \
+        __asm__ ("mulq %2"                  \
                 : "=a"(t1),"=d"(t2)     \
                 : "a"(a[i])             \
                 : "cc");                \
-        asm ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
+        __asm__ ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
                 : "+r"(c0),"+r"(c1),"+r"(c2)            \
                 : "r"(t1),"r"(t2),"g"(0)                \
                 : "cc");                                \
@@ -362,15 +362,15 @@ BN_ULONG bn_sub_words(BN_ULONG *r, BN_ULONG *a, BN_ULONG *b, int n)
 
 #  define mul_add_c2(a,b,c0,c1,c2) do { \
         BN_ULONG t1,t2;                 \
-        asm ("mulq %3"                  \
+        __asm__ ("mulq %3"                  \
                 : "=a"(t1),"=d"(t2)     \
                 : "a"(a),"m"(b)         \
                 : "cc");                \
-        asm ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
+        __asm__ ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
                 : "+r"(c0),"+r"(c1),"+r"(c2)            \
                 : "r"(t1),"r"(t2),"g"(0)                \
                 : "cc");                                \
-        asm ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
+        __asm__ ("addq %3,%0; adcq %4,%1; adcq %5,%2"       \
                 : "+r"(c0),"+r"(c1),"+r"(c2)            \
                 : "r"(t1),"r"(t2),"g"(0)                \
                 : "cc");                                \
diff --git a/crypto/bn/bn_div.c b/crypto/bn/bn_div.c
index 460d8b7..0331ecb 100644
--- a/crypto/bn/bn_div.c
+++ b/crypto/bn/bn_div.c
@@ -152,7 +152,7 @@ int BN_div(BIGNUM *dv, BIGNUM *rem, const BIGNUM *m, const BIGNUM *d,
     */
 #    undef bn_div_words
 #    define bn_div_words(n0,n1,d0)                \
-        ({  asm volatile (                      \
+        ({  __asm__ volatile (                      \
                 "divl   %4"                     \
                 : "=a"(q), "=d"(rem)            \
                 : "a"(n1), "d"(n0), "r"(d0)     \
@@ -167,7 +167,7 @@ int BN_div(BIGNUM *dv, BIGNUM *rem, const BIGNUM *m, const BIGNUM *d,
     */
 #    undef bn_div_words
 #    define bn_div_words(n0,n1,d0)                \
-        ({  asm volatile (                      \
+        ({  __asm__ volatile (                      \
                 "divq   %4"                     \
                 : "=a"(q), "=d"(rem)            \
                 : "a"(n1), "d"(n0), "r"(d0)     \
diff --git a/crypto/bn/bn_lcl.h b/crypto/bn/bn_lcl.h
index 6f7335b..87fc966 100644
--- a/crypto/bn/bn_lcl.h
+++ b/crypto/bn/bn_lcl.h
@@ -254,7 +254,7 @@ extern "C" {
 #   elif defined(__GNUC__) && __GNUC__>=2
 #    define BN_UMULT_HIGH(a,b)   ({      \
         register BN_ULONG ret;          \
-        asm ("umulh     %1,%2,%0"       \
+        __asm__ ("umulh     %1,%2,%0"       \
              : "=r"(ret)                \
              : "r"(a), "r"(b));         \
         ret;                    })
@@ -263,7 +263,7 @@ extern "C" {
 #   if defined(__GNUC__) && __GNUC__>=2
 #    define BN_UMULT_HIGH(a,b)   ({      \
         register BN_ULONG ret;          \
-        asm ("mulhdu    %0,%1,%2"       \
+        __asm__ ("mulhdu    %0,%1,%2"       \
              : "=r"(ret)                \
              : "r"(a), "r"(b));         \
         ret;                    })
@@ -273,13 +273,13 @@ extern "C" {
 #   if defined(__GNUC__) && __GNUC__>=2
 #    define BN_UMULT_HIGH(a,b)   ({      \
         register BN_ULONG ret,discard;  \
-        asm ("mulq      %3"             \
+        __asm__ ("mulq      %3"             \
              : "=a"(discard),"=d"(ret)  \
              : "a"(a), "g"(b)           \
              : "cc");                   \
         ret;                    })
 #    define BN_UMULT_LOHI(low,high,a,b)  \
-        asm ("mulq      %3"             \
+        __asm__ ("mulq      %3"             \
                 : "=a"(low),"=d"(high)  \
                 : "a"(a),"g"(b)         \
                 : "cc");
@@ -304,12 +304,12 @@ unsigned __int64 _umul128(unsigned __int64 a, unsigned __int64 b,
 #    else
 #     define BN_UMULT_HIGH(a,b) ({      \
         register BN_ULONG ret;          \
-        asm ("dmultu    %1,%2"          \
+        __asm__ ("dmultu    %1,%2"          \
              : "=h"(ret)                \
              : "r"(a), "r"(b) : "l");   \
         ret;                    })
 #     define BN_UMULT_LOHI(low,high,a,b)\
-        asm ("dmultu    %2,%3"          \
+        __asm__ ("dmultu    %2,%3"          \
              : "=l"(low),"=h"(high)     \
              : "r"(a), "r"(b));
 #    endif
@@ -318,7 +318,7 @@ unsigned __int64 _umul128(unsigned __int64 a, unsigned __int64 b,
 #   if defined(__GNUC__) && __GNUC__>=2
 #    define BN_UMULT_HIGH(a,b)   ({      \
         register BN_ULONG ret;          \
-        asm ("umulh     %0,%1,%2"       \
+        __asm__ ("umulh     %0,%1,%2"       \
              : "=r"(ret)                \
              : "r"(a), "r"(b));         \
         ret;                    })
diff --git a/crypto/md32_common.h b/crypto/md32_common.h
index b5a04bf..0eca897 100644
--- a/crypto/md32_common.h
+++ b/crypto/md32_common.h
@@ -166,7 +166,7 @@
    */
 #  if defined(__i386) || defined(__i386__) || defined(__x86_64) || defined(__x86_64__)
 #   define ROTATE(a,n)  ({ register unsigned int ret;   \
-                                asm (                   \
+                                __asm__ (                   \
                                 "roll %1,%0"            \
                                 : "=r"(ret)             \
                                 : "I"(n), "0"((unsigned int)(a))        \
@@ -176,7 +176,7 @@
 #  elif defined(_ARCH_PPC) || defined(_ARCH_PPC64) || \
         defined(__powerpc) || defined(__ppc__) || defined(__powerpc64__)
 #   define ROTATE(a,n)  ({ register unsigned int ret;   \
-                                asm (                   \
+                                __asm__ (                   \
                                 "rlwinm %0,%1,%2,0,31"  \
                                 : "=r"(ret)             \
                                 : "r"(a), "I"(n));      \
@@ -184,7 +184,7 @@
                         })
 #  elif defined(__s390x__)
 #   define ROTATE(a,n) ({ register unsigned int ret;    \
-                                asm ("rll %0,%1,%2"     \
+                                __asm__ ("rll %0,%1,%2"     \
                                 : "=r"(ret)             \
                                 : "r"(a), "I"(n));      \
                           ret;                          \
@@ -211,22 +211,22 @@
      * unaligned data without raising an exception.
      */
 #     define HOST_c2l(c,l)        ({ unsigned int r=*((const unsigned int *)(c)); \
-                                   asm ("bswapl %0":"=r"(r):"0"(r));    \
+                                   __asm__ ("bswapl %0":"=r"(r):"0"(r));    \
                                    (c)+=4; (l)=r;                       })
 #     define HOST_l2c(l,c)        ({ unsigned int r=(l);                  \
-                                   asm ("bswapl %0":"=r"(r):"0"(r));    \
+                                   __asm__ ("bswapl %0":"=r"(r):"0"(r));    \
                                    *((unsigned int *)(c))=r; (c)+=4; r; })
 #    endif
 #   elif defined(__aarch64__)
 #    if defined(__BYTE_ORDER__)
 #     if defined(__ORDER_LITTLE_ENDIAN__) && __BYTE_ORDER__==__ORDER_LITTLE_ENDIAN__
 #      define HOST_c2l(c,l)      ({ unsigned int r;              \
-                                   asm ("rev    %w0,%w1"        \
+                                   __asm__ ("rev    %w0,%w1"        \
                                         :"=r"(r)                \
                                         :"r"(*((const unsigned int *)(c))));\
                                    (c)+=4; (l)=r;               })
 #      define HOST_l2c(l,c)      ({ unsigned int r;              \
-                                   asm ("rev    %w0,%w1"        \
+                                   __asm__ ("rev    %w0,%w1"        \
                                         :"=r"(r)                \
                                         :"r"((unsigned int)(l)));\
                                    *((unsigned int *)(c))=r; (c)+=4; r; })
@@ -262,10 +262,10 @@
 # ifndef PEDANTIC
 #  if defined(__GNUC__) && __GNUC__>=2 && !defined(OPENSSL_NO_ASM) && !defined(OPENSSL_NO_INLINE_ASM)
 #   if defined(__s390x__)
-#    define HOST_c2l(c,l)        ({ asm ("lrv    %0,%1"                  \
+#    define HOST_c2l(c,l)        ({ __asm__ ("lrv    %0,%1"                  \
                                    :"=d"(l) :"m"(*(const unsigned int *)(c)));\
                                    (c)+=4; (l);                         })
-#    define HOST_l2c(l,c)        ({ asm ("strv   %1,%0"                  \
+#    define HOST_l2c(l,c)        ({ __asm__ ("strv   %1,%0"                  \
                                    :"=m"(*(unsigned int *)(c)) :"d"(l));\
                                    (c)+=4; (l);                         })
 #   endif
diff --git a/crypto/modes/modes_lcl.h b/crypto/modes/modes_lcl.h
index fe14ec7..dddabe8 100644
--- a/crypto/modes/modes_lcl.h
+++ b/crypto/modes/modes_lcl.h
@@ -39,33 +39,33 @@ typedef unsigned char u8;
 # if defined(__GNUC__) && __GNUC__>=2
 #  if defined(__x86_64) || defined(__x86_64__)
 #   define BSWAP8(x) ({ u64 ret_=(x);                   \
-                        asm ("bswapq %0"                \
+                        __asm__ ("bswapq %0"                \
                         : "+r"(ret_));   ret_;          })
 #   define BSWAP4(x) ({ u32 ret_=(x);                   \
-                        asm ("bswapl %0"                \
+                        __asm__ ("bswapl %0"                \
                         : "+r"(ret_));   ret_;          })
 #  elif (defined(__i386) || defined(__i386__)) && !defined(I386_ONLY)
 #   define BSWAP8(x) ({ u32 lo_=(u64)(x)>>32,hi_=(x);   \
-                        asm ("bswapl %0; bswapl %1"     \
+                        __asm__ ("bswapl %0; bswapl %1"     \
                         : "+r"(hi_),"+r"(lo_));         \
                         (u64)hi_<<32|lo_;               })
 #   define BSWAP4(x) ({ u32 ret_=(x);                   \
-                        asm ("bswapl %0"                \
+                        __asm__ ("bswapl %0"                \
                         : "+r"(ret_));   ret_;          })
 #  elif defined(__aarch64__)
 #   define BSWAP8(x) ({ u64 ret_;                       \
-                        asm ("rev %0,%1"                \
+                        __asm__ ("rev %0,%1"                \
                         : "=r"(ret_) : "r"(x)); ret_;   })
 #   define BSWAP4(x) ({ u32 ret_;                       \
-                        asm ("rev %w0,%w1"              \
+                        __asm__ ("rev %w0,%w1"              \
                         : "=r"(ret_) : "r"(x)); ret_;   })
 #  elif (defined(__arm__) || defined(__arm)) && !defined(STRICT_ALIGNMENT)
 #   define BSWAP8(x) ({ u32 lo_=(u64)(x)>>32,hi_=(x);   \
-                        asm ("rev %0,%0; rev %1,%1"     \
+                        __asm__ ("rev %0,%0; rev %1,%1"     \
                         : "+r"(hi_),"+r"(lo_));         \
                         (u64)hi_<<32|lo_;               })
 #   define BSWAP4(x) ({ u32 ret_;                       \
-                        asm ("rev %0,%1"                \
+                        __asm__ ("rev %0,%1"                \
                         : "=r"(ret_) : "r"((u32)(x)));  \
                         ret_;                           })
 #  endif
